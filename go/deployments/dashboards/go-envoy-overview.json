{
  "annotations": { "list": [] },
  "editable": true,
  "gnetId": null,
  "graphTooltip": 0,
  "links": [],
  "panels": [
    {
      "title": "Gateway Traffic (Requests per Minute)",
      "type": "timeseries",
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "targets": [
        {
          "datasource": { "type": "prometheus", "uid": "VictoriaMetrics" },
          "expr": "increase(envoy_http_downstream_rq_total{job=\"envoy-gateway\"}[1m])",
          "legendFormat": "Reqs/min ({{ envoy_response_code_class }})"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "custom": {
            "lineWidth": 2,
            "fillOpacity": 15,
            "showPoints": "auto"
          },
          "unit": "short"
        }
      },
      "description": "Shows absolute number of requests per minute (increase). Better for low traffic dev environments."
    },
    {
      "title": "Go Memory Details",
      "type": "timeseries",
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 },
      "targets": [
        {
          "datasource": { "type": "prometheus", "uid": "VictoriaMetrics" },
          "expr": "process_resident_memory_bytes{job=\"$service\"}",
          "legendFormat": "Physical RAM (RSS)"
        },
        {
          "datasource": { "type": "prometheus", "uid": "VictoriaMetrics" },
          "expr": "go_memstats_alloc_bytes{job=\"$service\"}",
          "legendFormat": "App Heap (Code)"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "unit": "bytes",
          "custom": { "fillOpacity": 10 }
        }
      }
    },
    {
      "title": "Active Goroutines",
      "type": "stat",
      "gridPos": { "h": 4, "w": 6, "x": 0, "y": 8 },
      "targets": [
        {
          "datasource": { "type": "prometheus", "uid": "VictoriaMetrics" },
          "expr": "go_goroutines{job=\"$service\"}",
          "legendFormat": "Goroutines"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "color": { "mode": "thresholds" },
          "thresholds": {
            "mode": "absolute",
            "steps": [
              { "color": "green", "value": null },
              { "color": "orange", "value": 100 },
              { "color": "red", "value": 1000 }
            ]
          }
        }
      }
    },
    {
      "title": "Recent Traces (Tempo)",
      "type": "table",
      "gridPos": { "h": 8, "w": 18, "x": 6, "y": 8 },
      "datasource": { "type": "tempo", "uid": "Tempo" },
      "targets": [
        {
            "refId": "A",
            "queryType": "nativeSearch",
            "limit": 20
        }
      ],
      "description": "List of recent traces. If empty, generate traffic."
    },
    {
      "title": "Live Logs",
      "type": "logs",
      "gridPos": { "h": 10, "w": 24, "x": 0, "y": 16 },
      "datasource": { "type": "loki", "uid": "${Loki}" },
      "targets": [
        {
          "datasource": { "type": "loki", "uid": "${Loki}" },
          "expr": "{job=~\"$service|envoy-gateway\"}"
        }
      ],
      "options": {
        "showTime": true,
        "showLabels": false,
        "wrapLogMessage": true,
        "sortOrder": "Descending"
      }
    }
  ],
  "refresh": "5s",
  "schemaVersion": 38,
  "tags": ["observability"],
  "templating": {
    "list": [
      {
        "name": "Loki",
        "type": "datasource",
        "query": "loki",
        "label": "Loki Datasource",
        "hide": 0,
        "refresh": 1
      },
      {
        "name": "service",
        "type": "query",
        "datasource": { "type": "prometheus", "uid": "VictoriaMetrics" },
        "query": "label_values(go_goroutines, job)",
        "refresh": 1,
        "sort": 1,
        "multi": false,
        "includeAll": false,
        "label": "Service",
        "hide": 0
      }
    ]
  },
  "time": { "from": "now-15m", "to": "now" },
  "title": "Microservices Overview (Final)",
  "uid": "microservices-overview",
  "version": 6
}
